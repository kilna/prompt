#!/bin/bash

__red_module_virtualenv() {
  local out="$(basename "$VIRTUAL_ENV" 2>/dev/null)"
  [[ "$out" ]] || return
  if __red_unicode; then
    echo -n "🐍"
  else
    echo -n 'py env '
  fi
  echo -n "$out"
}

__red_style_default_virtualenv() {
  echo -n '{fg:yellow}'
}

